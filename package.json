{
  "name": "idlechip-verifier",
  "version": "1.0.0",
  "description": "IdleChip GPU verifier - scan local GPUs and sync stats to the marketplace",
  "license": "MIT",
  "author": "lnctrl",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lnctrl/idlechip-verifier.git"
  },
  "homepage": "https://github.com/lnctrl/idlechip-verifier#readme",
  "bugs": {
    "url": "https://github.com/lnctrl/idlechip-verifier/issues"
  },
  "keywords": [
    "idlechip",
    "gpu",
    "nvidia",
    "marketplace",
    "cli"
  ],
  "bin": {
    "idlechip-verifier": "dist/cli.js"
  },
  "files": [
    "dist/cli.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "brand:apply": "node scripts/apply-brand.mjs",
    "build": "node scripts/build.mjs",
    "build:exe": "node scripts/build.mjs --exe",
    "prepublishOnly": "npm run build",
    "pack:check": "npm pack --dry-run"
  },
  "optionalDependencies": {
    "koffi": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  }
}
